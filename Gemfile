source "https://rubygems.org"
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

# Specify your gem's dependencies in refile_cdn_switcher.gemspec
gemspec

gem 'refile', require: 'refile/rails', github: 'refile/refile'
